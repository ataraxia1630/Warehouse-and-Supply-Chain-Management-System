NODE_ENV=development
PORT=3000

DATABASE_URL=postgresql://warehouse_user:warehouse_pass@db:5432/warehouse_db
MONGO_URL=mongodb://mongo_user:mongo_pass@mongodb:27017/warehouse_db?authSource=admin

# JWT Configuration (REQUIRED for auth module)
# Generate strong secrets for production use (minimum 10 characters)
JWT_ACCESS_SECRET=your-access-secret-key-here-min-10-chars
JWT_REFRESH_SECRET=your-refresh-secret-key-here-min-10-chars

# JWT Token Expiration (optional, defaults shown)
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Development Notes:
# - Copy this file to .env: cp .env.example .env
# - For local development, you can use the default values above
# - For production, generate strong random secrets (min 10 characters)
# - Database credentials match docker-compose.yml configuration
# - All services are containerized, no need to install PostgreSQL/MongoDB locally

